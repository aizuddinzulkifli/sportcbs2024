import 'package:flutter/material.dart';

class OpeningHoursScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Opening Hours'),
      ),
      body: Center(
        child: Text('Opening Hours Page Content'),
      ),
    );
  }
}